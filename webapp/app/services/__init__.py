"""The domain: how a novel gets planned, written, remembered and shipped.

These modules know what a story is. They sit above `core` and below `api`, and are
what `agents` are orchestrated by — the split from `core` is what keeps the wiring
readable now that there are twenty-five agents.
"""
